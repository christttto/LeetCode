class Solution {
    //so if not a mountain, returns 0. but no worries, it has been abstracted.
    public int peakIndexInMountainArray(int[] A) {
        //use bst type of approach
        //given the 
        
    }
}
