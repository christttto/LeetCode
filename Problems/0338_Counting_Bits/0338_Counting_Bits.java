class Solution {
    public int[] countBits(int num) {
        //so do the easy way first
        //turn the number binary, count all the 1
        //
        int returnList = new ArrayList<Integer>();
        returnList.add(0);
        if(num==0) return returnList;
        returnList.add(1);
        if(num==1) return returnList;
        int i=1;
        while(i<num){
            returnList.add(1+)
        }
        
        
    }
}